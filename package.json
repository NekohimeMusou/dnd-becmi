{
  "name": "dndbecmi",
  "version": "1.0.0",
  "description": "Unofficial FVTT implementation of the classic RPG.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc && copyfiles -u 1 src/**/*.css src/**/*.json src/**/*.webp src/**/*.hbs src/**/*.mp3 dist/ && copyfiles README.md dist/"
  },
  "author": "Nekohime Musou",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NekohimeMusou/dndbecmi"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/jquery": "^3.5.29",
    "@typescript-eslint/eslint-plugin": "^6.20.0",
    "@typescript-eslint/parser": "^6.20.0",
    "copyfiles": "^2.4.1",
    "eslint": "^8.56.0",
    "typescript": "^5.3.3"
  }
}
